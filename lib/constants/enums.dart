/// Enum to select target ThumbType
enum ThumbType {
  square,
  circle,
}


/// Enum to select the bcackground image animation transition
enum ImageTransitionAnimationStyle {
  scale,
  fade,
  none,
}


/// Enum to animated the icon on the thumb 
enum IconTransitionStyle {
  fade,
  scale,
  none,
}

/// Select a button ready sizes for both Switch || Slider
enum SwitcherSize {
  xSmall,
  small,
  large,
  medium,
  xLarge,
  xxLarge,
}
